# Runtime dependencies for the web server.
# GUI packages (customtkinter, matplotlib, mplfinance, plyer) are intentionally
# excluded — charts are rendered in the browser via Plotly.js.
requests==2.31.0
pandas==2.1.0
numpy==1.26.0
Pillow==10.1.0
beautifulsoup4==4.12.3
python-dotenv==1.0.0
fastapi
uvicorn[standard]
